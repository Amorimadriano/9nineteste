services:
  - type: web
    name: nfse-mtls-proxy
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: PROXY_PORT
        value: 10000
      - key: PROXY_API_KEY
        sync: false